jsonSchema: true
quiet: true
verbose: 5
lint:
  # additional rules files can be added here
  rules:
  - strict
  skip:
  - info-contact
  - short-summary
  - openapi-tags-alphabetical
resolve:
  output: swagger.yaml
