get:
  summary: Get job
  description: >
    Get more details about an `AsyncJob` and its associated `Transaction`s.
  parameters:
  - $ref: '../../parameters/parameters.yaml#/job/path'
  tags:
  - AsyncJobs
  responses:
    '200':
      description: "Success"
      content:
        application/vnd.api+json:
          schema:
            $ref: '../../schemas/job.yaml#/getResponse'
    '401':
      description: "Unauthorized"
      content:
        application/vnd.api+json:
          schema:
            $ref: '../../schemas/errors.yaml#/401'