get:
  summary: List device statuses
  description: >
    List all current `Device` statuses/configurations for an organization
  parameters:
    - $ref: '../../parameters/deviceParams.yaml#/active'
    - $ref: '../../parameters/deviceParams.yaml#/inBbox'
  tags:
    - Devices
  responses:
    '200':
      description: "Success"
      content:
        application/vnd.api+json:
          schema:
            $ref: '../../schemas/device.yaml#/listResponse'
    '401':
      description: "Unauthorized"
      content:
        application/vnd.api+json:
          schema:
            $ref: '../../schemas/errors.yaml#/401'