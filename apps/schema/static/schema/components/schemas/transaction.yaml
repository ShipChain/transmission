resource:
  properties:
    type:
      example: Transaction

attributes:
  allOf:
   - $ref: '#/transaction/hash'
   - $ref: '#/transaction/nonce'
   - $ref: '#/transaction/chainId'
   - $ref: '#/transaction/toAddress'
   - $ref: '#/transaction/value'
   - $ref: '#/transaction/gasLimit'
   - $ref: '#/transaction/gasPrice'
   - $ref: '#/transaction/data'

getResource:
  allOf:
  - $ref: '#/ethereum/resourceId'
  - $ref: '#/resource'
  - properties:
      attributes:
        $ref: '#/attributes'

ethereum:
  $ref: dataTypes/ethereum.yaml

transaction:
  $ref: dataTypes/ethereum.yaml#/transaction
